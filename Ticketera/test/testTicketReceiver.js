/*
//TODO: revisar después, voy con los tests para agregar grupos al sistema
var assert = require('assert');

const ticketReceiverModule = require('../model/TicketReceiver');
const ticketModule = require('../model/Ticket');
const employeeModule = require('../model/Employee');

//const system = new systemModule.System([]);
const anna = new employeeModule.Employee('Anna', 'Smith', '@anna', 'Developer');
const ticketReceiver = new w.TicketReceiver('#devs');
const topic = 'bug #1';
const content = 'test content';
const state = 'pendiente';
const priority = 'alto';
const ticket = new tc.Ticket('@anna', '#devs', topic, content, state, priority);



describe('TicketReceiver', function() {
    describe('#getEmployees()', function() {
        it('should return empty array', function() {
            assert.equal(workgroup.getEmployees().length, 0);
        });
  });


    describe('#getEmployees()', function() {
        it('should return true if the array get one employee', function() {
            workgroup.addEmployee(employee);
            assert.equal(workgroup.getEmployees().length, 1);
        });
    });


});
*/